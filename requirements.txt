# =============================================================================
# PariShiksha — Retrieval-Ready Study Assistant
# Week 9 Mini-Project · PG Diploma AI-ML & Agentic AI Engineering
# =============================================================================

# --- PDF Extraction & Text Processing ---
PyMuPDF>=1.23.0           # fitz — fast, reliable PDF text extraction
pdfplumber>=0.10.0        # alternative PDF extractor for comparison
pytesseract>=0.3.10       # OCR fallback for image-heavy pages

# --- NLP & Tokenization ---
transformers>=4.36.0      # Hugging Face tokenizers, models (BERT, T5, etc.)
tokenizers>=0.15.0        # Fast tokenizer library
sentencepiece>=0.1.99     # SentencePiece tokenizer (used by T5)
tiktoken>=0.5.0           # OpenAI BPE tokenizer for comparison
nltk>=3.8.1               # Sentence tokenization, text utilities
spacy>=3.7.0              # Sentence segmentation, NER

# --- Vector Database & Industrial Retrieval ---
chromadb>=0.4.0           # Persistent vector database
rank-bm25>=0.2.2          # Probabilistic keyword ranking
faiss-cpu>=1.7.0          # Optimized vector search indexing
sentence-transformers>=2.2.2   # SBERT for dense embeddings
scikit-learn>=1.3.0            # TF-IDF, cosine similarity, metrics
numpy>=1.24.0
scipy>=1.11.0

# --- LLM & Generative Models ---
google-generativeai>=0.3.0     # Gemini API for decoder-only generation
# openai>=1.6.0                # Optional: OpenAI API

# --- Evaluation ---
rouge-score>=0.1.2        # ROUGE metrics for answer evaluation
bert-score>=0.3.13        # BERTScore for semantic similarity

# --- Data Handling & Visualization ---
pandas>=2.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0           # Pretty-print tables in terminal

# --- Utilities ---
python-dotenv>=1.0.0      # .env file loading
tqdm>=4.66.0              # Progress bars
pyyaml>=6.0               # YAML config parsing
loguru>=0.7.0             # Structured logging

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0

# --- Jupyter ---
jupyter>=1.0.0
ipywidgets>=8.1.0
